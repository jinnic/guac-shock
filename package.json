{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "deploy:gh-pages": "npm run build && touch dist/.nojekyll && npx gh-pages -d dist --dotfiles",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.3"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
